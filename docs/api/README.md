# API Documentation Placeholder

This folder will contain auto-generated API docs for the `sona` Python package.

To generate docs locally using pdoc:

```powershell
pip install pdoc3
pdoc --output-dir docs/api --force sona
```

Once generated, commit the `docs/api/` folder or host via GitHub Pages.
