# v0.12.0 Release Checklist

## Scope

- [x] Runtime memory public boundary slimmed to the stable model layer.
- [x] `advanced` and `internal` runtime memory surfaces added.
- [x] First-party interpreter/runtime imports updated to the new boundary.
- [x] Runtime architecture note added for language-first positioning.
- [x] One low-risk nested `.smod` quick win added.
- [x] Bug sweep restricted to release blockers.
- [x] Core/stdlb/extension version metadata aligned to `0.12.0`.
- [x] Release docs drafted under `docs/release-notes/`.

## Verification

- [x] Import-boundary smoke test locks the reduced public runtime-memory surface.
- [x] Goal continuation persists a complete deterministic explanation envelope before continuation proceeds.
- [x] `tests/test_smod_native_modules.py tests/test_smod_metadata_contracts.py -q`
- [x] `tests/runtime -q`
- [x] `tests/test_cli_errors.py test_direct_execution.py -q`
- [x] `python -m sona --version`
- [x] `python -m build ... --sdist --wheel` completed with `sona-0.12.0.tar.gz` and `sona-0.12.0-py3-none-any.whl` produced.
- [x] Built wheel installed and validated in an isolated venv outside the repo root.
- [x] Nested VS Code extension artifact rebuilt as `sona-ai-native-programming-0.12.0.vsix`.

## Release Lock

- [x] Freeze scope to the intended 0.12.0 payload.
- [x] Finalize GitHub release body.
- [x] Review extension dependency advisories before publishing VSIX artifacts.
- [ ] Tag and publish release.

## Validation Notes

- `python -m sona --version` -> `Sona 0.12.0 (AI Features 1.0.0)`
- `python -m pytest -o addopts='' tests/runtime/test_memory_public_api.py tests/runtime/test_memory_goal_loop.py -q` -> `20 passed`
- `python -m pytest -o addopts='' tests/test_smod_native_modules.py tests/test_smod_metadata_contracts.py -q` -> `14 passed`
- `python -m pytest -o addopts='' tests/runtime -q` -> `84 passed`
- `python -m pytest -o addopts='' tests/test_cli_errors.py test_direct_execution.py -q` -> `1 passed`
- Isolated wheel install resolved to `site-packages`, not the repo checkout.
- Nested VSIX rebuild succeeded: `vscode-extension/sona-ai-native-programming/sona-ai-native-programming-0.12.0.vsix`
- Root `vscode-extension/` wrapper package still carries `axios`; do not publish that wrapper artifact as part of `0.12.0`.

## Scope Guard

- `0.12.0` is a boundary and packaging release, not a runtime-platform rewrite.
- Export-boundary correction and version coherence are in scope.
- Large deferred roadmap work remains out of scope.
