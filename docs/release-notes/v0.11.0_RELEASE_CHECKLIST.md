# v0.11.0 Release Checklist

> **Note:** v0.10.4 was absorbed into v0.11.0. All v0.10.4 deliverables are tracked here.

## Kickoff

- [x] Opened `0.11.0` version line in core package metadata.
- [x] Opened `0.11.0` version line in CLI/runtime constants.
- [x] Opened `0.11.0` version line in VS Code extension manifest.
- [x] Created draft release docs (`v0.11.0.md`, GitHub body, checklist).

## Delivery Scope — Governance Baseline (from v0.10.4)

- [x] Receipt chain verification (`sona receipt verify-chain`) implemented.
- [x] Governance anchors in receipt header (`policy_fingerprint`, `engine_policy_fingerprint`, `prev_receipt_hash`, `receipt_hash`).
- [x] Control-plane modules restored (`sona.policy`, `sona.lockfile_manager`).
- [x] Cross-platform CI smoke matrix (Windows/Linux/macOS).
- [x] Spec draft baselines published (`DETERMINISM_DRAFT_1.md`, `RECEIPT_SPEC_0.3_DRAFT.md`).
- [x] Wave-1 `.smod` metadata contracts (`json`, `fs`, `io`, `path`, `env`).

## Delivery Scope — Native `.smod` Architecture

- [x] `.smod` loading pipeline: `_resolve_smod_path()` → `_load_smod_module()` → `import_module()`.
- [x] `NativeBridge` and `NativeModuleProxy` bridge architecture in `native_bridge.py`.
- [x] Pure-Sona (`smod`) vs bridge (`smod-bridge`) runtime categories established.
- [x] Package manager `.smod` support in `spm.py`.
- [x] Wave-2 metadata: `queue`, `stack`, `random`, `uuid`, `regex` (pure), `hashing` (bridge).
- [x] Wave-3 metadata: `csv`, `date`, `math`, `string`, `time` — backlog cleared to zero.
- [x] All 16 stdlib `.smod` modules have explicit `module_format` / `runtime_backend` metadata.
- [x] Runtime-level `.smod` modules: `receipt.smod`, `emotion.smod`.
- [x] Sub-module: `stdlib/utils/convert.smod`.

## Delivery Scope — Trust-Layer Hardening

- [x] Receipt signing baseline implemented (`receipt sign` / `receipt verify-signature`).
- [x] Deterministic redaction baseline implemented (`receipt redact` / `receipt redact-dir`).
- [x] Trust-layer ledger enhancements completed and documented.
- [x] Determinism/replay coverage expanded and passing.
- [x] No regressions in `sona policy`, `sona lock`, and `sona receipt` flows.

## Delivery Scope — New Modules

- [x] Receipt context runtime (`receipt.smod`, `native_receipt.py`).
- [x] Emotion module stack (`emotion.smod`, `emotion.py`, `native_emotion.py`).
- [x] Stdlib manifest updated (`MANIFEST.json`, `stdlib_cap` → 95).

## Verification Commands (Update As Needed)

- `python -m sona --version`
- `python -m pytest tests/test_receipt_integrity.py -q` (47 tests)
- `python -m pytest tests/test_lockfile_manager.py -q` (3 tests)
- `python -m pytest tests/test_smod_native_modules.py -q` (7 tests)
- `python -m pytest tests/test_smod_metadata_contracts.py -q` (5 tests, all 3 waves + exhaustive scan)
- `python -m pytest tests/test_emotion_module.py -q` (30 tests)
- Full suite: 92 passed

## Release Lock (When Feature Complete)

- [x] Freeze scope and update `docs/release-notes/v0.11.0.md`.
- [x] Finalize `docs/release-notes/v0.11.0_GITHUB_RELEASE_BODY.md`.
- [x] Update `docs/release-notes/CHANGELOG.md` with final dated entry.
- [ ] Build and verify VSIX artifact for target extension version.
- [ ] Tag and publish release branch/tag.
