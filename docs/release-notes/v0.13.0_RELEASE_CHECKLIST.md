# v0.13.0 Release Checklist

## Scope

- [x] `sona.runtime.memory.policy` — deterministic runtime policy engine (PolicyKernel, MemoryPolicy, 5 sub-policies).
- [x] `sona.runtime.memory.audit` — append-only governance audit log (AuditKernel, AuditEvent, AuditSummary).
- [x] `sona.runtime.memory.compliance` — read-only compliance reporter (ComplianceReporter, ComplianceReport, ComplianceViolation).
- [x] Interpreter wired: `runtime_audit_kernel` and `runtime_compliance_reporter` created in constructor.
- [x] Audit integration in `intake.py`, `inspection.py`, `goal_loop.py`.
- [x] Governance types exported from `sona.runtime.memory.advanced`.
- [x] 31 governance tests created and passing (policy 6 + audit 16 + compliance 9).
- [x] Version metadata aligned to `0.13.0` across core, stdlib, and VS Code extension.
- [x] Release docs drafted under `docs/release-notes/`.

## Verification

- [x] `python -m pytest -o addopts='' tests/runtime/test_memory_policy.py -q` -> `6 passed`
- [x] `python -m pytest -o addopts='' tests/runtime/test_memory_audit.py -q` -> `16 passed`
- [x] `python -m pytest -o addopts='' tests/runtime/test_memory_compliance.py -q` -> `9 passed`
- [x] `python -m pytest -o addopts='' tests/runtime -q` -> `118 passed`
- [x] `get_errors` on audit.py, compliance.py, interpreter.py, advanced.py -> `0 errors`
- [x] `python -m sona --version` -> `Sona 0.13.0 (AI Features 1.0.0)`
- [x] `python -m build --sdist --wheel --no-isolation` completed with `sona_lang-0.13.0.tar.gz` and `sona_lang-0.13.0-py3-none-any.whl` produced.
- [x] Built wheel installed and validated in an isolated venv outside the repo root.
- [x] Nested VS Code extension artifact rebuilt as `sona-ai-native-programming-0.13.0.vsix`.

## Release Lock

- [x] Freeze scope to the intended 0.13.0 payload.
- [x] Finalize GitHub release body.
- [ ] Review extension dependency advisories before publishing VSIX artifacts.
- [ ] Tag and publish release.

## Scope Guard

- `0.13.0` is a governance release, not a runtime-platform rewrite.
- Policy enforcement, audit logging, and compliance reporting are in scope.
- No breaking changes to the public language surface.
- Large deferred roadmap work remains out of scope.
