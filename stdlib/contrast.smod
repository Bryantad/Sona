const module_format = "smod-runtime"
const runtime_backend = "smod"

func configure(options) { return experimental_configure(options) }
func status() { return experimental_status() }
func ratio(foreground, background) { return contrast_ratio(foreground, background) }
func recommend(foreground, background) { return contrast_recommend(foreground, background) }
