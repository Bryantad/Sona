const module_format = "smod-runtime"
const runtime_backend = "smod"

func set(mode) {
    return pace_set(mode)
}

func current() {
    return pace_current()
}

func configure(options) {
    return pace_configure(options)
}

func format(text) {
    return pace_format(text)
}
