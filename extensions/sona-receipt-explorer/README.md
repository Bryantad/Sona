# Sona Receipt Explorer: Execution Audit Trail

[![Version](https://img.shields.io/badge/version-0.13.0-blue.svg)](https://github.com/Bryantad/Sona/releases)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Marketplace](https://img.shields.io/badge/VS%20Code-Marketplace-blue)](https://marketplace.visualstudio.com/items?itemName=Waycoreinc.sona-receipt-explorer)
[![Part of Sona](https://img.shields.io/badge/part%20of-Sona%20Ecosystem-purple)](https://github.com/Bryantad/Sona)

---

A Visual Studio Code extension for **visualizing, comparing, and managing Sona execution receipts**. Part of the **Sona cognitive accessibility ecosystem**.

> Every Sona execution can generate a deterministic receipt — this extension makes them explorable.

---

## Overview

Sona 0.13.0 keeps the **Execution Receipts** workflow stable while adding runtime governance primitives underneath the same language-first surface. Receipts remain JSON files that capture:

- Script path and arguments
- Start/end timestamps with duration
- Exit code and status
- Environment snapshot
- Output capture (optional)

The Receipt Explorer brings these receipts into VS Code with a **visual tree view**, **rich detail panels**, and **diff comparison**.

---

## Key Features

### Tree View Sidebar

- **Date Grouping** — receipts organized by Today, Yesterday, This Week, Older
- **Status Icons** — ✓ success, ✗ failure, ⚠ warning at a glance
- **Click to Open** — instant navigation to receipt details

### Rich Detail Panel

- **Execution Summary** — script, args, duration, exit code
- **Environment Snapshot** — Python version, working directory, env vars
- **Trust Metadata** — receipt hash chain, policy fingerprints, signature status, and redaction profile
- **Output Viewer** — stdout/stderr with syntax highlighting
- **Timing Breakdown** — parse, execute, cleanup phases

### Comparison Tools

- **Diff Two Receipts** — side-by-side comparison of any two executions
- **Highlight Changes** — spot differences in output, timing, or environment
- **Track Regressions** — compare before/after when debugging

### Workflow Integration

- **Explore Receipts** — jump straight into the Sona Receipts view from the command palette
- **Re-Run from Receipt** — execute the same command with one click
- **Copy Command** — get the exact CLI invocation
- **Export as Markdown** — document execution history
- **Verify Receipt Chain** — validate receipt-directory hash-chain integrity using the Sona CLI
- **Export Receipt Bundle** — package a portable bundle with chain data and optional workspace lockfile payload
- **Delete Old Receipts** — clean up with confirmation

---

## Commands

| Command                | Description                                                       |
| ---------------------- | ----------------------------------------------------------------- |
| Explore Receipts       | Reveal the receipts activity view and refresh receipt discovery   |
| Refresh Receipts       | Reload receipt list from workspace                                |
| Open Receipt           | View receipt in detail panel                                      |
| Compare Receipts       | Diff two selected receipts                                        |
| Re-Run from Receipt    | Execute the recorded command                                      |
| Copy Command           | Copy CLI command to clipboard                                     |
| Open Raw Receipt       | Open original receipt JSON file                                   |
| Delete Receipt         | Remove with confirmation                                          |
| Export as Markdown     | Generate documentation                                            |
| Verify Receipt Chain   | Run `sona receipt verify-chain` against the active receipts folder |
| Export Receipt Bundle  | Run `sona receipt export` and open the resulting bundle JSON       |
| Verify Signature       | Validate receipt signature                                        |
| Redact Receipt         | Produce deterministic redacted copy                               |

---

## Settings

```json
{
  "sonaReceipts.autoRefresh": true,
  "sonaReceipts.maxReceipts": 100,
  "sonaReceipts.groupByDate": true,
  "sonaReceipts.receiptGlob": "**/*.receipt.json",
  "sonaReceipts.statusFilter": "all"
}
```

---

## Receipt Format

Receipts are JSON files generated by `sona --receipt`:

```json
{
  "version": "0.1",
  "script": "demo.sona",
  "args": ["--verbose"],
  "timestamp": "2025-01-20T10:30:00Z",
  "duration_ms": 142,
  "exit_code": 0,
  "status": "success",
  "environment": {
    "python_version": "3.12.0",
    "sona_version": "0.13.0",
    "working_dir": "/project"
  }
}
```

---

## Use Cases

### Debugging

> "What changed between yesterday's run and today's?"

Compare receipts to spot environment or output differences.

### CI/CD Verification

> "Did the nightly build produce the same output?"

Receipts provide deterministic proof of execution.

### Audit Trail

> "When was this script last run successfully?"

Browse execution history with timestamps and status.

### Reproducibility

> "I need to run exactly what worked before"

Re-run from receipt with the exact same parameters.

---

## Part of the Sona Ecosystem

Receipt Explorer integrates with Sona's execution model:

- **`sona --receipt`** — generate receipts on every run
- **`sona --receipt-env`** — include environment snapshot
- **`sona receipt verify-chain`** — validate hash-chain integrity across a receipts directory
- **`sona receipt export`** — emit a portable receipt bundle with chain metadata
- **`sona receipt verify-signature`** — validate signed receipts
- **`sona receipt redact`** — emit deterministic redacted copies
- **Deterministic Execution** — same input → same receipt

---

## Requirements

- VS Code 1.74.0 or higher
- Sona 0.13.0 or higher (for aligned governance metadata and receipt tooling)

## Deterministic Packaging

Build a byte-stable VSIX artifact:

```bash
npm run package:vsix
```

Verify reproducibility by building twice and comparing SHA256:

```bash
npm run package:vsix:verify
```

---

## Links

- [Sona Language](https://github.com/Bryantad/Sona)
- [Execution Receipts Documentation](https://github.com/Bryantad/Sona/blob/main/API.md)
- [Report Issues](https://github.com/Bryantad/Sona/issues)
- [Changelog](CHANGELOG.md)

---

## License

MIT License — see [LICENSE](LICENSE) for details.

---

<p align="center">
  <i>Built with 🧾 by WayCore Inc.</i>
</p>

### Trust & Governance (0.13.0)

- Verify receipt signatures (Sona Receipts: Verify Receipt Signature).
- Verify directory-level receipt chains (Sona Receipts: Verify Receipt Chain).
- Export portable receipt bundles (Sona Receipts: Export Receipt Bundle).
- Redact receipts with selectable profiles (dev/ci/prod) and emit redacted copies.
- Tree items show receipt hash, policy fingerprint, signature, and redaction metadata in tooltips.
- Detail, diff, and markdown export views now include receipt-chain, policy, signature, and redaction fields.
