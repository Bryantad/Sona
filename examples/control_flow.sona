let score = 82;
let grade = when {
    score >= 90 => "A";
    score >= 80 => "B";
    _ => "Keep practicing";
};

print("Grade: " + grade);

let count = 3;

while count > 0 {
    print("Countdown: " + count);
    count = count - 1;
};

print("Control flow complete");
