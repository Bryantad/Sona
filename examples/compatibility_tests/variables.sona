// Variables test for Sona v0.4.9
import utils.math.smod

// Basic variable declarations
print("Testing variable declarations:")
let x = 5
let name = "Sona"
let pi_value = math.PI
let is_active = 1  // 1 for true, 0 for false

print("x = " + math.to_str(x))
print("name = " + name)
print("pi_value = " + math.to_str(pi_value))
print("is_active = " + math.to_str(is_active))

// Variable reassignment
print("\nTesting variable reassignment:")
let x = 10  // Note: 'let' is required for reassignment
print("New x = " + math.to_str(x))

// Arrays
print("\nTesting arrays:")
let numbers = [1, 2, 3, 4, 5]
print("Array length: " + math.to_str(math.len(numbers)))

// Accessing array elements
print("First element: " + math.to_str(numbers[0]))
print("Third element: " + math.to_str(numbers[2]))

// String variables
let first_name = "John"
let last_name = "Doe"
let full_name = first_name + " " + last_name
print("\nFull name: " + full_name)

// Important notes
print("\nVariable Notes for v0.4.9:")
print("1. Always use 'let' for variable declaration AND reassignment")
print("2. Convert numbers to strings with math.to_str() when concatenating")
print("3. Boolean values use 1 for true and 0 for false")
print("4. Arrays are zero-indexed")
