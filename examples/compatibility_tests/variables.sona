// Variables test for Sona v0.5.1+
import utils.math.smod

// Basic variable declarations
print("Testing variable declarations:")
let x = 5
let name = "Sona"
let pi_value = math.PI
let is_active = true  // Boolean literals now supported

print("x = " + str(x))
print("name = " + name)
print("pi_value = " + str(pi_value))
print("is_active = " + str(is_active))

// Variable reassignment
print("\nTesting variable reassignment:")
x = 10  // No 'let' needed for reassignment
print("New x = " + str(x))

// Multiple reassignments
x = x + 5
x = x * 2
print("After operations: " + str(x))

// Arrays
print("\nTesting arrays:")
let numbers = [1, 2, 3, 4, 5]
print("Array length: " + str(len(numbers)))

// Accessing array elements
print("First element: " + str(numbers[0]))
print("Third element: " + str(numbers[2]))

// String variables
let first_name = "John"
let last_name = "Doe"
let full_name = first_name + " " + last_name
print("\nFull name: " + full_name)

// String reassignment
first_name = "Jane"
full_name = first_name + " " + last_name
print("Updated full name: " + full_name)

// Important notes
print("\nVariable Notes for v0.5.1+:")
print("1. Use 'let' for variable declaration only")
print("2. Reassignment works without 'let'")
print("3. Boolean literals 'true' and 'false' are supported")
print("4. Built-in str() function available")
print("5. Arrays are zero-indexed")
