// Test file for Sona import syntax
import native_stdin
import utils.math.smod as math
import utils.array.smod as array
import utils.string.smod as string

print("Testing imports...")
print("Math PI: " + math.to_str(math.PI))
print("Array test: " + array.to_str([1, 2, 3]))
print("String uppercase: " + string.upper("hello"))
