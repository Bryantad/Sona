// Testing importing math module
import utils.math.smod

// Using the module directly by its base name (smod or math)
print("Testing math module directly...")
print("2 + 3 = " + smod.to_str(smod.add(2, 3)))
