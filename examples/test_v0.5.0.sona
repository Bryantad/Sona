// Comprehensive test for Sona v0.5.0 features
import utils.math.smod as math

// Feature 1: Multi-line string support
let welcome_message = """
Welcome to Sona v0.5.0!
This is a multi-line string test.
It supports line breaks and preserves formatting.
"""

print(welcome_message)

// Feature 2: Function with proper syntax and using 'as' import
func add_numbers(a, b) {
    return math.add(a, b)
}

// Test function call
let result = add_numbers(10, 20)
print("10 + 20 = " + math.to_str(result))

// Feature 3: Inline comments at end of statements
let pi = math.PI  // This is the value of PI

// Feature 4: Better error reporting (will be evident in interpreter output)
print("Math module PI value: " + math.to_str(pi))

print("Test complete!")
