// Test file with corrected imports for Sona v0.4.9
import native_stdin

// Import math utility
import utils.math.smod
// Use directly
print("Math PI: " + utils.math.smod.to_str(utils.math.smod.PI))

// Try if statement
if utils.math.smod.eq(1, 1) {
    print("1 equals 1")
} else {
    print("This should not happen")
}

// Test if-else chain
let value = 2
if utils.math.smod.eq(value, 1) {
    print("Value is 1")
} else {
    if utils.math.smod.eq(value, 2) {
        print("Value is 2")
    } else {
        print("Value is something else")
    }
}
